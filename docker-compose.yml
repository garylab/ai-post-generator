services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8000}:8000"
    env_file: .env
    environment:
      DB_URL: ${DB_URL_DOCKER}
    extra_hosts:
      - "host.docker.internal:host-gateway"
